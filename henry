var serviceURL = "http://www.apstrata.com/callHttp_auth_test/ok.html";
var authCredsMap = new Array("test","test");

var http = require("http");

var options = {
		url: serviceURL,
		authCreds: authCredsMap,
		method: "POST"
};

var returnResult = http.request(options);
return returnResult;
